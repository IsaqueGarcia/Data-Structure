package linkedList.leetcode;

public class hasLoopExam {

   /* public boolean hasLoop(){
        //Floyd's method
        Node fast = head;
        Node slow = head;

        while(fast != null && fast.next != null){
            fast = fast.next.next; //2 steps ahead
            slow =  slow.next; //1 step

            if(slow == fast){
                return true;
            }
        }
        return false;
    }*/

}
